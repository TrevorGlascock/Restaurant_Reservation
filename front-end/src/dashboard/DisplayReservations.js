import React from "react";

/**
 * Defines the front-end table displaying appropriate reservations.
 * @param reservations
 *  the current array of reservation objects to display
 * @returns {JSX.Element}
 */
export default function DisplayReservations({ reservations }) {
  return <table></table>;
}
